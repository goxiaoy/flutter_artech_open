import 'package:flutter/material.dart';

class ForwardIcon extends Icon {
  const ForwardIcon({Color? color, double? size})
      : super(Icons.arrow_forward_ios,
            color: color ?? Colors.grey, size: size ?? 18.0);
}
